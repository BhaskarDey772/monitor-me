{
  "name": "monitor-me",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "license": "ISC",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "check-types": "turbo run check-types",
    "db:generate": "turbo run db:generate",
    "db:migrate": "pnpm --filter api db:migrate",
    "auth:generate": "pnpm --filter api auth:generate",
    "db:deploy": "pnpm --filter api db:deploy",
    "db:studio": "pnpm --filter api db:studio",
    "user:create": "pnpm --filter api user:create"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@11.0.9",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "11.0.9",
      "onFail": "download"
    }
  }
}
